version: 0.0
os: linux
files:
  - source: helloworld
    destination: /app/
hooks:
  BeforeInstall:
  - source: script/stop.sh
    timeout: 300
    runas: root
  AfterInstall:
  - source: script/start.sh
    timeout: 300
    runas: root